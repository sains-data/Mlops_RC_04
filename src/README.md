# MLOps Pothole Detection Project

This project implements a complete MLOps pipeline for pothole detection.

## Quick Start

```python
# Install dependencies
pip install -r requirements.txt

# Validate dataset
python cli.py validate-data

# Train model
python cli.py train --model yolov8n

# Start API
python cli.py serve
```

See SETUP.md for detailed instructions.
